/**
 * Variáveis
 */

// Criando variáveis
let carro
let modelo
let ano

// Atribuindo valor a uma variável
carro = "honda"
modelo = "civic"
ano = 2023

// Chamando variáveis no console com node usando template string
console.log(
    `A marca do carro é ${carro}, modelo ${modelo} e ano ${ano}.`
)

// Criando variáveis e dando valores
let nome = "Aricelio Moura"
let idade = 18
let atividade = "analista"

console.log(
    `Olá, meu nome é ${nome}, tenho ${idade} anos e exerço a função de ${atividade}.`
)